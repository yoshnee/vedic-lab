/* twitter-image — the X/Twitter share card. Same art as the Open Graph card;
   Next.js wires <meta name="twitter:image"> (and twitter:card = summary_large_image)
   from this convention file. */
export { default, alt, size, contentType } from "./opengraph-image";
